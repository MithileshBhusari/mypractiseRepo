server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: file:///C:/practise/java/microservice_using_springboot/cloud-config-server/cloud-config-git/config-server.git
          clone-on-start: true